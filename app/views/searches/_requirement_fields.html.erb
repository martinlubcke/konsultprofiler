<div class="fields">
  <%= f.select :value, requirement_pairs %>
  <% if f.object.skill %>
    <%= f.object.skill.name %>
  <% else %>
  	<%= chained_skill_selects_for :search, :requirements %>
  <% end %>
  <%= link_to_remove_fields "Ta bort", f %>
</div>
	