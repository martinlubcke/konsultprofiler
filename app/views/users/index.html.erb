<h1>Användare</h1>

<p><b>OBS!</b> De användare som kopplats till profiler redigeras genom profilen.</p>

<ul>
	<% @users.each do |user| %>
	  <li><%= link_to h(user.name), user_path(user) %> (<%=h user.login %>, <%=h user.email %>)</li>
	<% end %>	
</ul>

<%= ([link_to 'Ny användare', new_user_path] + standard_links).join ' | '%>